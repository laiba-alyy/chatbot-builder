// middleware/rateLimiter.js
// Placeholder rate-limiting middleware. Replace with a real limiter such as
// express-rate-limit or a Redis-based solution when you need throttling.

module.exports = (req, res, next) => {
  // simply pass through for now
  next();
};
